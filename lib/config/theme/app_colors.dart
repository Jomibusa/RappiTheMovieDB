import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color seed = Colors.deepPurple;

  static const Color error = Colors.red;
  static const Color rating = Colors.amber;

  static const Color imagePlaceholderDark = Color(0xFF212121);
  static const Color imagePlaceholderIcon = Colors.white24;

  static const Color skeletonBaseDark = Color(0xFF424242);
  static const Color skeletonBaseLight = Color(0xFFE0E0E0);
}
